/// <reference types="next" />
/// <reference types="next/image-types/global" />

// Production typecheck entrypoint. `next-env.d.ts` can be rewritten by
// `next dev` to reference `.next/dev` artifacts, which are not stable inputs
// for CI or Vercel builds.
