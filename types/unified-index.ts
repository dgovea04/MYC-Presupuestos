export type UnifiedIndexRelationRow = {
  code: string;
  name: string;
  resourceCount: number;
};

export type UnifiedIndexDictionaryRow = {
  code: string;
  element: string;
  note: string | null;
};
