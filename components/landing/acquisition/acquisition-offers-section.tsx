import { Check } from "lucide-react";
import { AcquisitionCta } from "@/components/landing/acquisition/acquisition-cta";
import { acquisitionOffers } from "@/components/landing/acquisition/acquisition-landing-content";

export function AcquisitionOffersSection() {
  return <section id="planes" className="scroll-mt-24 bg-slate-50 py-20 md:py-28"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div className="max-w-2xl"><span className="inline-flex rounded-full border border-blue-100 bg-blue-50 px-3 py-1 text-xs font-semibold uppercase tracking-[0.18em] text-blue-700">Empieza sin fricción</span><h2 className="mt-4 text-3xl font-semibold tracking-tight text-slate-950 sm:text-4xl">Un camino claro desde tu primer presupuesto hasta Pro.</h2><p className="mt-4 text-base leading-7 text-slate-600">Starter es el CTA principal. Pro tiene un precio fundador anual para los primeros usuarios: luego será S/ 349/año.</p></div><div className="mt-10 grid gap-5 lg:grid-cols-3">{acquisitionOffers.map((offer) => <article key={offer.name} className={`rounded-[1.75rem] border p-6 ${offer.highlighted ? "border-blue-200 bg-slate-950 text-white shadow-xl shadow-blue-950/10" : "border-slate-200 bg-white"}`}><p className={`text-sm font-semibold uppercase tracking-[0.16em] ${offer.highlighted ? "text-blue-300" : "text-blue-700"}`}>{offer.name}</p><p className="mt-5 text-3xl font-semibold tracking-tight">{offer.price}</p><p className={`mt-3 min-h-12 text-sm leading-6 ${offer.highlighted ? "text-slate-300" : "text-slate-600"}`}>{offer.description}</p><div className="mt-6 space-y-3">{offer.features.map((feature) => <p key={feature} className={`flex items-start gap-2 text-sm ${offer.highlighted ? "text-slate-200" : "text-slate-700"}`}><Check className={`mt-0.5 h-4 w-4 shrink-0 ${offer.highlighted ? "text-blue-300" : "text-emerald-600"}`} />{feature}</p>)}</div><AcquisitionCta href={offer.href} location={`acquisition_offer_${offer.name.toLowerCase().replaceAll(" ", "_")}`} variant={offer.highlighted ? "primary" : "secondary"} className="mt-8 w-full">{offer.cta}</AcquisitionCta></article>)}</div></div></section>;
}
