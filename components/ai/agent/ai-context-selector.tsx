"use client";

import { useId } from "react";

type Props = { provider: string; model: string; scope: "AUTO" | "USER" | "WORKSPACE" | "PROJECT" | "TEAM"; onProviderChange: (value: string) => void; onModelChange: (value: string) => void; onScopeChange: (value: Props["scope"]) => void; disabled?: boolean };
const providers = [{ value: "openai", label: "OpenAI" }, { value: "gemini", label: "Gemini" }, { value: "openrouter", label: "OpenRouter" }];
export function AiContextSelector({ provider, model, scope, onProviderChange, onModelChange, onScopeChange, disabled = false }: Props) { const base = useId(); return <div className="grid gap-3 rounded-2xl border border-[var(--app-border)] bg-[var(--app-surface-muted)] p-3 sm:grid-cols-3"><label htmlFor={`${base}-provider`} className="space-y-1 text-xs font-medium">Proveedor<select id={`${base}-provider`} disabled={disabled} value={provider} onChange={(e) => onProviderChange(e.target.value)} className="mt-1 w-full rounded-lg border bg-[var(--app-surface)] px-2 py-1.5 text-sm">{providers.map((item) => <option key={item.value} value={item.value}>{item.label}</option>)}</select></label><label htmlFor={`${base}-model`} className="space-y-1 text-xs font-medium">Modelo<input id={`${base}-model`} disabled={disabled} value={model} onChange={(e) => onModelChange(e.target.value)} placeholder="Modelo predeterminado" className="mt-1 w-full rounded-lg border bg-[var(--app-surface)] px-2 py-1.5 text-sm" /></label><label htmlFor={`${base}-scope`} className="space-y-1 text-xs font-medium">Quién paga<select id={`${base}-scope`} disabled={disabled} value={scope} onChange={(e) => onScopeChange(e.target.value as Props["scope"])} className="mt-1 w-full rounded-lg border bg-[var(--app-surface)] px-2 py-1.5 text-sm"><option value="AUTO">Automático</option><option value="PROJECT">Proyecto</option><option value="TEAM">Equipo</option><option value="WORKSPACE">Workspace</option><option value="USER">Mi usuario</option></select></label></div>; }
