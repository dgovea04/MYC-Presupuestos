"use client";
import { PrivateLearningExampleList } from "./private-learning-example-list";
export function PrivateLearningSettings({ enabled, examples, canManage, onToggle, onRevoke }: { enabled: boolean; examples: readonly { id: string; signalType: string; sourceType: string; status: string; expiresAt: string }[]; canManage: boolean; onToggle: (enabled: boolean) => void; onRevoke?: (id: string) => void }) { return <section aria-label="Aprendizaje privado" className="space-y-4 rounded-2xl border border-slate-200 bg-white p-5"><div><h2 className="text-base font-semibold text-slate-900">Aprendizaje privado por empresa</h2><p className="mt-1 text-sm text-slate-500">Reutiliza decisiones confirmadas solo dentro de este workspace. No entrena un modelo global.</p></div><label className="flex items-center gap-2 text-sm text-slate-700"><input type="checkbox" checked={enabled} disabled={!canManage} onChange={(event) => onToggle(event.target.checked)} /> Activar recuperación privada</label><p className="text-xs text-slate-500">Retención predeterminada: 365 días. Las sugerencias no modifican el presupuesto.</p><PrivateLearningExampleList examples={examples} onRevoke={canManage ? onRevoke : undefined} /></section>; }
