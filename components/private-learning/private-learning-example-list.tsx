"use client";
type Example = { id: string; signalType: string; sourceType: string; status: string; expiresAt: string };
export function PrivateLearningExampleList({ examples, onRevoke }: { examples: readonly Example[]; onRevoke?: (id: string) => void }) { return <div className="space-y-2">{examples.map((example) => <div key={example.id} className="flex items-center justify-between rounded-xl border border-slate-200 px-3 py-3"><div><p className="text-sm font-medium text-slate-800">{example.signalType}</p><p className="text-xs text-slate-500">Origen: {example.sourceType} · vence {new Date(example.expiresAt).toLocaleDateString()}</p></div>{example.status === "ACTIVE" && onRevoke && <button type="button" onClick={() => onRevoke(example.id)} className="text-xs text-red-600">Revocar</button>}</div>)}</div>; }
