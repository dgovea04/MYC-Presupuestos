"use client";
import { useEffect, useState } from "react";
import { Activity, Loader2 } from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
type Report = { summary: { requests: number; tokens: number; actualCostMinor: number }; byProvider: Array<{ provider: string; model: string; requests: number; tokens: number; actualCostMinor: number }>; bySource: Array<{ credentialSource: string | null; billingScope: string | null; requests: number; tokens: number; actualCostMinor: number }> };
export function WorkspaceAiUsageDashboard({ workspaceId }: { workspaceId: string }) { const [provider, setProvider] = useState(""); const [report, setReport] = useState<Report | null>(null); const [error, setError] = useState(""); useEffect(() => { let cancelled = false; const query = provider ? `?workspaceId=${encodeURIComponent(workspaceId)}&provider=${encodeURIComponent(provider)}` : `?workspaceId=${encodeURIComponent(workspaceId)}`; void fetch(`/api/admin/ai-usage${query}`, { cache: "no-store" }).then(async (response) => { const payload: unknown = await response.json(); if (!response.ok) throw new Error("No se pudo cargar el desglose de uso."); if (!cancelled) setReport(payload as Report); }).catch((reason: unknown) => { if (!cancelled) setError(reason instanceof Error ? reason.message : "No se pudo cargar el uso."); }); return () => { cancelled = true; }; }, [workspaceId, provider]); return <Card><CardHeader><CardTitle className="flex items-center gap-2"><Activity className="h-5 w-5" />Desglose avanzado de IA</CardTitle></CardHeader><CardContent className="space-y-4">{error ? <p className="text-sm text-rose-700">{error}</p> : null}<label className="block text-xs font-medium">Proveedor<select value={provider} onChange={(event) => setProvider(event.target.value)} className="mt-1 w-full rounded-xl border px-3 py-2"><option value="">Todos</option><option value="OPENAI">OpenAI</option><option value="GEMINI">Gemini</option><option value="OPENROUTER">OpenRouter</option></select></label>{!report ? <Loader2 className="h-4 w-4 animate-spin" /> : <><div className="grid gap-3 sm:grid-cols-3"><Metric label="Solicitudes" value={report.summary.requests} /><Metric label="Tokens" value={report.summary.tokens} /><Metric label="Costo" value={`${(report.summary.actualCostMinor / 100).toFixed(2)}`} /></div><div className="space-y-2">{report.byProvider.map((row) => <div key={`${row.provider}-${row.model}`} className="flex justify-between rounded-xl border p-3 text-xs"><span>{row.provider} · {row.model}</span><span>{row.tokens} tokens · {row.requests} solicitudes</span></div>)}</div></>}</CardContent></Card>; }
function Metric({ label, value }: { label: string; value: number | string }) { return <div className="rounded-xl border p-3"><p className="text-xs text-[var(--app-text-muted)]">{label}</p><strong className="text-lg">{value}</strong></div>; }
