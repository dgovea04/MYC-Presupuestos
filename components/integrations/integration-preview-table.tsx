"use client";
type PreviewRow = { externalKey: string; action: string; description?: string; conflict?: string };
export function IntegrationPreviewTable({ rows }: { rows: readonly PreviewRow[] }) { return <div className="overflow-x-auto rounded-xl border border-slate-200"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-3 py-2">Clave</th><th className="px-3 py-2">Acción</th><th className="px-3 py-2">Descripción</th><th className="px-3 py-2">Estado</th></tr></thead><tbody>{rows.map((row) => <tr key={row.externalKey} className="border-t border-slate-100"><td className="px-3 py-2 font-mono text-xs">{row.externalKey}</td><td className="px-3 py-2">{row.action}</td><td className="px-3 py-2">{row.description ?? "—"}</td><td className={`px-3 py-2 ${row.conflict ? "text-red-600" : "text-emerald-600"}`}>{row.conflict ?? "Listo"}</td></tr>)}</tbody></table></div>; }
