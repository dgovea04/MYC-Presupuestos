import { NextResponse } from "next/server";
import { getAuthSession } from "@/lib/auth/session";
import { resolveBudgetOwnership } from "@/lib/collaboration/authorization";
import { transitionIntegrationSession } from "@/lib/integrations/sessions";
import { prisma } from "@/lib/db/prisma";
export async function POST(_request: Request, { params }: { params: Promise<{ id: string; sessionId: string }> }) { const session = await getAuthSession(); if (!session) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); try { const { id, sessionId } = await params; const { companyId } = await resolveBudgetOwnership(id, session.user.id); const budget = await prisma.budget.findUnique({ where: { id }, select: { updatedAt: true } }); if (!budget) return NextResponse.json({ error: "Presupuesto no encontrado" }, { status: 404 }); await prisma.integrationSession.updateMany({ where: { id: sessionId, companyId, budgetId: id }, data: { expectedVersion: Math.floor(budget.updatedAt.getTime() / 1000) } }); await transitionIntegrationSession({ sessionId, companyId, to: "STAGED" }); return NextResponse.json({ session: await transitionIntegrationSession({ sessionId, companyId, to: "VALIDATED" }) }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "No se pudo validar" }, { status: 400 }); } }
